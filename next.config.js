/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ISR-friendly, no need for custom image domains unless you host cover images
};

module.exports = nextConfig;
